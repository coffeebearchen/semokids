generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Run {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())

  inputJson  String
  outputJson String
}